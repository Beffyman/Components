stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: '⚙️ Compile'
        dependsOn: [  ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Restore Compile --skip'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: Nuget
              pathtoPublish: 'Nuget'
      - job: Test
        displayName: '🚦 Test'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Test --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: Tests
              pathtoPublish: 'Tests'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: coverage.cobertura.xml
              pathtoPublish: 'coverage.cobertura.xml'
      - job: Report
        displayName: '📄 Report'
        dependsOn: [ Test ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Report --skip'
      - job: Performance
        displayName: '⏲️ Performance'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Performance --skip'
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: Compile
        displayName: '⚙️ Compile'
        dependsOn: [  ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Restore Compile --skip'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: Nuget
              pathtoPublish: 'Nuget'
      - job: Test
        displayName: '🚦 Test'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Test --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: Tests
              pathtoPublish: 'Tests'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: coverage.cobertura.xml
              pathtoPublish: 'coverage.cobertura.xml'
      - job: Report
        displayName: '📄 Report'
        dependsOn: [ Test ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Report --skip'
      - job: Performance
        displayName: '⏲️ Performance'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Performance --skip'
  - stage: macOS_latest
    displayName: 'macOS-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'macOS-latest'
    jobs:
      - job: Compile
        displayName: '⚙️ Compile'
        dependsOn: [  ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Restore Compile --skip'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: Nuget
              pathtoPublish: 'Nuget'
      - job: Test
        displayName: '🚦 Test'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Test --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: Tests
              pathtoPublish: 'Tests'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: coverage.cobertura.xml
              pathtoPublish: 'coverage.cobertura.xml'
      - job: Report
        displayName: '📄 Report'
        dependsOn: [ Test ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Report --skip'
      - job: Performance
        displayName: '⏲️ Performance'
        dependsOn: [ Compile ]
        steps:
          - task: PowerShell@2
            inputs:
              filePath: 'build.ps1'
              arguments: 'Performance --skip'
